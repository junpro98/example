package com.framework.spring.controller;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.ModelAttribute;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;


/*
@Controller
public class HelloController {
	
	
	@RequestMapping("/hello")
	public String cont() {
		return "/WEB-INF/webFile/framwork.jsp";
	}
	@RequestMapping(value="/hello2", method=RequestMethod.POST)
	public String test01(@RequestParam String hi) { // 이 메소드를 통해 실행되면 parameter값을 넣어준걸 어노테이션 해준다.
		System.out.println("Param" + hi);
		return "/WEB-INF/webFile/framwork2.jsp";
	}
//	@RequestMapping("/hello3") 아래 어노테이션은 DTO에 있는 GET SET을 알아서 찾아간다.
//	public String test02(@ModelAttribute DTO framworkDTO) { 지금은 DTO를 만들어 둔게 없기때문에 주석처리했음.
//		return "WEB-INF/webFile/framwork3.jsp";
//	}
}

*/
